# Shared AgentPostmortem Supabase project (Tracecase owns the tc_* tables).
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Shared secret CI must send in the x-tracecase-token header to POST runs.
TRACECASE_INGEST_TOKEN=change-me
